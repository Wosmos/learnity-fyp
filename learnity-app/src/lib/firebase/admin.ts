/**
 * Firebase Admin SDK Export Module
 * Re-exports Firebase Admin services for consistent import paths
 */

export { adminApp, adminAuth, adminStorage } from '@/lib/config/firebase-admin';
export { default } from '@/lib/config/firebase-admin';